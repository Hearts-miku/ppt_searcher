# 📦 PPT 智能检索器 - 完整客户端与服务端打包指南 (Packaging Guide)

本项目采用 **React 18 前端 (Vite) + Express 后端 + Electron 本地壳** 的高内聚本地混合架构。无论您是想将应用发布为 **Windows 独立安装包 (.exe)** 面向桌面用户分发，还是想将其部署为 **Web 服务端常驻应用**，本指南都将为您提供全方位、工业级的实战指导。

---

## 🗺️ 打包系统架构一览

为了在生产环境下提供单文件、零摩擦的部署体验，我们在 `package.json` 和构建流中做了深度重构优化：

```
                        [ 代码仓库源码 ]
                               │
               ┌───────────────┴───────────────┐
               ▼ (npm run build)               ▼ (npm run build)
      【React 18 前端编译】            【Express 后端单文件束】
   Vite 将 JSX/CSS 编译压缩         esbuild 将 TS 语法进行类型抹除
  输出至 -> dist/ (index.html)     合并依赖输出至 -> dist/server.cjs
               │                               │
               └───────────────┬───────────────┘
                               ▼
                    [ 双核编译静态成果物 ]
                               │
       ┌───────────────────────┴───────────────────────┐
       ▼ 方案 A：Web 独立服务端部署                  ▼ 方案 B：Windows 安装包打包 (Electron)
     直接启动 node dist/server.cjs                  使用 electron-builder 将环境、内核
     极速运行于物理主机、Docker、云服务器          与 dist/、main.cjs 联合压合为 Setup.exe
```

---

## 🖥️ 方案 A：桌面客户端打包 (Windows `.exe` 独立安装包)

这是**最推荐**的分发方式。它能将 Chromium 浏览器、Node.js 运行环境、Express 后台服务以及所有的前端页面资产打入一个不超过 **80MB** 的安全绿色安装包中，用户安装后双击桌面图标即可离线运行。

### 1️⃣ 第一步：前置依赖准备
将此项目代码克隆或下载到您的**本地 Windows 电脑**后，打开您的终端（PowerShell 或 CMD），并在项目根目录下安装打包所需的开发依赖：
```bash
npm install electron electron-builder --save-dev
```

### 2️⃣ 第二步：确认主要入口文件
在您的项目根目录下，我们已经帮您准备好了高可靠性的 **`main.cjs`**（Electron 主进程入口）。它自带：
- **安全自动找港**：自动探测并寻找本地空闲的 TCP 端口启动 Express 进程，从源头上杜绝端口冲突。
- **后台静默拉起**：主程序启动时，将在后台平滑加载并无感温启动前端页面及后端 PowerShell 宿主。
- **开发者面板热唤醒**：预置了 `Ctrl + Shift + I` 快捷键，极度便于开发期与灰度用户在桌面上直接按键呼出控制台打印日志。

### 3️⃣ 第三步：执行打包命令
在项目根目录下，在终端中输入并执行以下一键封箱命令：
```bash
npm run pack-app
```

### 4️⃣ 第四步：收获您的独立程序
打包程序大约需要 30 到 90 秒进行依赖项剪裁与固化，成功后系统会在项目根目录下自动创建名为 **`release/`** 的文件夹。
- **`PPT智能检索器 Setup.exe`**：这是您最终得到的**高集成一键安装包**。
- 您可以将此 `.exe` 直接拷贝分发给公司的跨部门领导、合作伙伴、同事。
- **体验**：他们双击该安装包，按提示选择安装路径。安装完成后，桌面上将全自动生成一个富有科技感的启动快捷图标，启动即用！

---

## 🌐 方案 B：独立 Web 服务端打包 (Linux / CentOs / Docker 云部署)

如果您希望在公司局域网群晖 (NAS)、内网集中式应用服务器或公有云主机上运行此系统，作为常驻的 PPT 语义大脑，请遵照此方案。

### 1️⃣ 第一步：编译前端与后端代码
在您的多端部署平台上配置好 Node.js >= 18 环境，并在控制台执行：
```bash
# 1. 自动装载所有必需模块
npm install

# 2. 调用 esbuild 与 vite 进行物理提纯与静态优化
npm run build
```
此命令执行完毕后，所有的关键逻辑已全部存储于 **`dist/`** 目录中。

### 2️⃣ 第二步：将成果提取并交付至生产服务器
在生产服务端，为了保持最极致的纯净性，您其实**不需要**把完整的源代码压缩包上传。您只需要拷贝以下生产交付产物：
1. **`dist/`** 目录（内含静态页面和高度压缩的 `server.cjs` 单文件）。
2. **`package.json`**（定义了版本信息和 `start` 命令）。
3. **`node_modules/`**（运行所需的外部包。如已在生产机装过，直接在生产机执行 `npm install --omit=dev` 即可自动秒装）。

### 3️⃣ 第三步：常驻后台启动
为了让服务不随着终端会话的关闭而退出，您可以采用主流的 **PM2 专业守护程序** 来进行监控启动：
```bash
# 1. 全局安装 pm2 守护平台
npm install -g pm2

# 2. 启动服务并给它起一亮丽名字
pm2 start dist/server.cjs --name "ppt-neural-engine"

# 3. 设置跟随服务器重启开机自启
pm2 save
pm2 startup
```
此时，您可以通过内网 IP 访问：`http://您的服务器IP:3000`，使整个局域网下的团队成员共同共享 PPT 检索大脑。

---

## 🎨 视觉与品牌属性个性化定制 (图标/名称自定义)

若您希望更换应用的图标或名称，打上属于您公司或业务的专属烙印，可直接在本地按以下要素修改：

### 1. 修改软件显示名称
在项目根目录下的 **`package.json`** 中，修改以下配置字段：
- `"productName"`: `"您的创意检索器名称"` (例如：`“文稿大脑”`)
- `"build.nsis.shortcutName"`: `"桌面快捷方式名称"`

### 2. 精美应用图标替换 (Icon Customization)
当前打包构建模块已为您关联并锁定了打包资源：
- **Windows 图标**：准备一张高画质的图标，转换或导出为标准的 **`.ico`** 格式，放到项目根目录下的 `public/` 目录中。
- **关联定位**：打开 `package.json`，将 56 行处的 `"icon": "public/vite.svg"` 后缀更替为您全新的图标路径：
  ```json
  "win": {
    "target": ["nsis"],
    "icon": "public/your_awesome_icon.ico"
  }
  ```

---

## 🛠️ 高频疑难排查与性能调优指南

### Q1: 打包出来的可执行文件点击运行后白屏，并报 `ERR_CONNECTION_REFUSED` 报错？
**答案**：
1. **原因所在**：这几乎 100% 是由于 Express 后台服务崩溃或未成功拉起，导致界面壳子 Chromium 找不到物理响应端口。
2. **本底层优化**：我们已经针对此问题在 `server.ts` 中完成了深度加固。之前某些代码直接在顶部 `import` 了 `vite` 库，而生产打包时 `vite` 依赖已经被剔除，导致 Node.js 直接抛出“Cannot find module 'vite'”服务崩溃。
3. **排错方案**：我们已经将 `vite` 改为了**动态、按需、仅开发态温重载加载**，已杜绝此故障。如您仍遭遇此状态，可直接使用快捷键 `Ctrl + Shift + I` 唤醒控制台，查看具体的连接或脚本报错日志。

### Q2: 杀毒软件（如 Windows Defender、360 等）爆安全提醒？
**答案**：
* 由于打包生成的安装包是由开源社区著名工具 `electron-builder` 压合而成的独立 `.exe`。
* 任何未向微软申请昂贵的每年商业数字证书认证（Digital Signature）的本地二进制执行程序，Windows Defender 在初次拦截解析时都会贴上“位置发布者”标签。
* 本软件 100% 遵守开源协议，无任何后台回传行为，离线亦可完美运行，只需点击“更多信息” -> “仍要运行”便能毫无隐忧地展开文稿之旅。

### Q3: 为什么 Windows 下有时候双击卡片无法立即跳转并打开 PPT 的特定页码？
**答案**：
1. **进程隔离**：请确保您的 PPT 检索器没有通过“以管理员身份运行”启动，而您的 PowerPoint 软件又是普通权限启动的。两者的权限级不一致会导致 Windows 系统的 COM 通信与 UAC 拒绝指令交互。
2. **完美状态**：检索器与 PowerPoint 都保持默认的双击普通用户账户权限打开，即可建立 100% 完美的飞梭高亮联动。
