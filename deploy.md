# PPT 智能检索器 - 本地运行方案与极速部署指南

本项目采用全栈混合架构（Vite + React 前端 + Express 后端）。当您将项目代码导出并存储于 GitHub 仓库后，可以按照以下步骤轻松下载并在您的本地机器、开发服务器上部署运行。

---

## 🛠 一、 运行准备环境

在开始之前，请确保您的本地计算机已安装以下基础工具：
1. **Node.js**：建议使用 Node.js `v18.x` 或更新的 LTS 版本。你可以通过 `node -v` 命令检验是否安装妥当。
2. **Git**：用于克隆或从 GitHub 拉取最新代码。

---

## 🚀 二、 极速本地部署运行步骤

### 1. 克隆代码仓库
首先，打开您的系统终端（Terminal/PowerShell），并将代码拉取到您的本地磁盘目录下：
```bash
git clone <您的 GitHub 仓库地址>
cd <项目文件夹名称>
```

### 2. 安装项目依赖
执行 `npm install` 自动从 registry 镜像源拉取前端与后端所必需的所有开发包（如 Express、adm-zip、Chokidar、Vite 等）：
```bash
npm install
```

### 3. 热配置本地密钥信息（可选）
如果您希望在云端在线大模型词分类场景下运行，可在本地项目的根目录下新建一个名为 `.env` 的配置文件，并填下以下内容：
```env
# 云端 Google Gemini 大模型服务调用凭证 Key
GEMINI_API_KEY=您的_GEMINI_API_KEY
```
*提示：如果您的电脑环境或涉及的业务涉密较高，也可以完全不配置 API Key。在前端高级设置对话框中将策略变更为「本地离线模式」，系统即可启用 100% 局域电磁离线隔绝搜索能力，零网络波动畅顺运行。*

### 4. 运行方案 A：本地极速开发热重载模式 (Development Mode)
在此模式下，系统通过后端原生调度 TypeScript 执行器启动 Express，同时搭载 Vite 瞬态热重载。如果您在编辑器里修改了前端 JSX 代码或后台脚本，开发服务器会实时感知并更新：
```bash
npm run dev
```
- 控制台在启动后，输出：`Server running on port 3000`
- 打开浏览器输入：**`http://localhost:3000`**，即可直接开始您非凡的 PPT 检索。

### 5. 运行方案 B：生产环境编译打包发布 (Production Build & Run)
如果您想获得极致加载性能体验，或准备将此检索服务作为常驻守护管线运行在公司内网服务器上：
```bash
# 1. 编译构建。此命令将编译前端 SPA 静态产物至 dist 开发区，
# 并且通过 esbuild 闪电构建出无冗余依赖的后端单文件 dist/server.cjs
npm run build

# 2. 启动编译好后的标准 CJS 工程
npm start
```

---

## 💡 三、 常见配置与问题排查

1. **首次打开显示「暂无任何文件同步目录」？**
   - 没关系，为了让您在首次运行时就能体验检索效果，系统启动时会自动在主程序目录下建立一个名为 `sample_ppts` 的预置文件夹并填入三篇优质样例幻灯片文稿，随后将其自动授信成监控根目录。
   - 您可以直接在左侧栏「本地磁盘浏览器」中穿梭至任何一个属于新 PPT 演示文稿的大文件夹，点击其右侧的「添加工作监控」图标，后台会瞬间捕获这些新文件夹下的内容，并在数秒内完成哈希计算与增量向量化拼装入库。
2. **幻灯片「双击」拉起自动飞梭的运行原理是什么？**
   - 本地运行时，如果您使用的是 **Windows 操作系统**，系统后端将自动通过 PowerShell 调用您电脑装有的本地微软 Office。
   - 如果您使用的是 **macOS 操作系统**，则通过系统脚本互操作逻辑 AppleScript 来闪电唤醒 PPT 并高亮精确定位。这也完全避免了您再去层层打开磁盘资源管理器的繁杂手工开销。
