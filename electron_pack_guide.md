# PPT 智能检索器 - 打包为 Windows 独立的安装包 (.exe) 教学指南

由于本项目采用了 **React 前端 + Node.js (Express) 后端** 的双端本地化混合架构（用于执行 PowerShell/AppleScript 本地 PowerPoint 飞梭联动以及 Chokidar 本地磁盘扫描），最优秀的 Windows 客户端打包方案是使用 **Electron + electron-builder** 框架。

下面为您提供最详细、配有完整脚手架代码的客户端化打包策略。

---

## 📦 打包架构设计精要

在打包成的 `.exe` 程序运行中：
1. **主进程 (Electron Main Process)**：启动时在后台静默拉起合并编译好的后端服务进程 `dist/server.cjs`（自动寻找可用本地空闲端口）。
2. **渲染进程 (Electron Renderer)**：创建一个无边框毛玻璃窗口，直接载入本地启动的 Express 端点，带给用户 100% 极速、纯粹的本地桌面大屏端应用体验。

---

## 🛠 开发步骤说明

> **✨ 好消息**：为了最大限度地简化您的本地打包体验，本项目中**已经预先帮您创建并配置好了**主入口文件 `main.cjs`，且同步修改了 `package.json` 中的各打包配置字段。
>
> 当您将代码下载至本地后，只需按以下步骤运行极速封箱：

### 步骤 1：安装 Electron 所需打包依赖
在您拉取至本地的代码**项目根目录（即含有 `package.json` 的文件夹）**下，打开系统终端命令行（CMD、PowerShell 或 Terminal），并运行以下命令添加客户端开发打包工具：
```bash
npm install electron electron-builder --save-dev
```

### 步骤 2：检查或微调主入口文件 `main.cjs`（已帮您完全创建好）
在您项目根目录下的 **`main.cjs`** 文件已预配置好双端关联、可用端口探测、后台启动 Express 等核心逻辑。如果在本地有特定需求（如需调整默认 1200x800 的窗口尺寸、添加额外的原生菜单等），可以直接在这个文件中进行修改。

---

### 步骤 3：打包配置字段（已帮您在 `package.json` 中配置好）
`package.json` 中已经为您预设了 **三个核心字段**，使项目具备完整的生产级桌面软件打包能力：
1. **`main`**: 已设为 `"main.cjs"`
2. **`scripts`**: 已增加 `"pack-app": "npm run build && electron-builder"` 极速流打包命令
3. **`build`**: 预置了标准的打包定义，包含图标源、打包包含的文件（`main.cjs`、`dist`、`node_modules`）以及 Windows 的 NSIS 安装向导等。

---

## 🏃‍♂️ 执行 Windows 极速封箱打包！

做好以上准备步骤配置后，在您的**项目根目录（即含有 `package.json` 的主文件夹）**下，在终端中闪电执行这句简短的打包指令：

```bash
npm run pack-app
```

### 🥳 打包完成后得到的绝妙产物
打包构建流程将在数十秒至一分钟内拉起、构建并封箱。构建成功后，在您的**项目根目录下**会自动生成一个名为 **`release/`** 的文件夹。在这个文件夹中：
1. **`PPT智能检索器 Setup.exe`（或类似的安装程序）**：这是可以直接拷贝并发给您的同事、企业跨部门领导或客户的一键式 Windows 标准安装包！
2. 双击该安装向导，它将在 Windows 桌面上生成精美的应用快捷图标，启动即用，完美连通。

---

## 🔄 四、 后面每次版本更新都需要重新打一次包吗？

**简单回答**：是的。如果您对系统的**前端交互界面**（如 React 组件、CSS 布局样式）或**后端处理逻辑**（如 Express 的 API、检索权重算法、本地 PowerPoint 定位脚本）进行了修改，都需要重新执行打包来使最终的可执行 `.exe` 文件生效。

这是因为打包程序（`npm run pack-app`）具有以下物理编译流程：
1. **构建前端产物**：运行 `vite build` 把所有 React 前页代码编译压缩为轻量、高速读取的静态资产。
2. **构建后端单文件**：运行 `esbuild` 把服务侧代码高度混淆封装为一个高集成度、免配 Node 的单包文件 `dist/server.cjs`。
3. **Electron 外壳固化**：将最新编译出的前端、后端产物和微型浏览器容器共同压合并装入全新的独立应用程序外壳。

### ⚡ 极速版本迭代发布流（仅消耗 1 分钟）：
当您未来在 GitHub 代码仓库中更新了代码后，在您本地项目机器上的操作流程无比流畅：
1. **拉取最新代码**：
   ```bash
   git pull
   ```
2. **在项目根目录下，直接键入闪电打包命令**：
   ```bash
   npm run pack-app
   ```
3. **获取升级版安装包**：打包完成后，将 `release/` 文件夹下生成的新版 `.exe` 再次发给同事，他人双击安装即可直接覆盖老版本，且安全可靠，用户数据（如已导入的工作台、监控记录等）也会因缓存完全得以平滑传承。

---

## 💡 性能调优建议

1. **体积精简优化**：由于 Electron 会封装一整个 Chromium 内核，打包后安装包通常在 60MB - 100MB 左右（属于正常业界体积）。您可以配置 npm `devDependencies` 使一些开发工具依赖仅在构建期起作用，无需打入生产。
2. **Windows Defender 误报说明**：我们在打包 `serverProcess` 时设置了 Windows 静默窗口运行（`windowsHide: true`），在首次分发到公司涉密电脑安装时，Windows 可能会弹出防病毒通知提示，直接点击「仍要运行」即可放心自给，100% 安全纯物理内网数据闭环。
